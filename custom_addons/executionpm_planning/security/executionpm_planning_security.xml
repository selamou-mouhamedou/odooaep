<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Approved planning documents are read-only for everyone except admins -->
    <record id="rule_execution_planning_readonly_approved" model="ir.rule">
        <field name="name">Execution Planning: Read-only when Approved</field>
        <field name="model_id" ref="model_execution_planning"/>
        <field name="domain_force">[('state', '=', 'approved')]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
        <field name="groups" eval="[(4, ref('executionpm_core.group_executionpm_base'))]"/>
    </record>

    <!-- PMO: Modify Planning when in Draft or Submitted -->
    <record id="rule_execution_planning_pmo_edit" model="ir.rule">
        <field name="name">PMO: Edit Planning</field>
        <field name="model_id" ref="model_execution_planning"/>
        <field name="groups" eval="[(4, ref('executionpm_core.group_executionpm_pmo'))]"/>
        <field name="domain_force">[('state', 'in', ['draft', 'submitted'])]</field>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="1"/>
        <field name="perm_create" eval="1"/>
        <field name="perm_unlink" eval="1"/>
    </record>

</odoo>
